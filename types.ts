
export interface Certificate {
  id: string;
  title: string;
  issuer: string;
  date: string;
  imageUrl: string;
  description: string;
}

export interface BrandInfo {
  name: string;
  tagline: string;
  logoColor: string;
}
